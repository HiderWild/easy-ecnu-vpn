{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the EXV Tauri window: core IPC (invoke) + event listen + log.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "log:default"
  ]
}
